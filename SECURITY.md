# 安全政策

## 支持范围

安全修复优先针对默认分支和 README 当前锁定的 Codex CLI、飞书 CLI 版本。其他版本未经审计，桥接器可能拒绝启动，也不承诺安全兼容。

## 报告漏洞

请不要为尚未修复的安全问题创建公开 Issue，也不要附带真实凭据、飞书事件、会话内容或可识别个人的信息。

请使用代码托管平台的私密漏洞报告功能联系维护者。仓库发布后应在平台中启用 Private vulnerability reporting；在该功能启用前，不应公开披露可利用细节。

报告中建议包含：

- 受影响版本或提交；
- 攻击前提和信任边界；
- 最小化复现步骤；
- 可能影响；
- 可行的缓解或修复建议。

维护者确认问题后应尽快回复影响范围和处理计划。修复发布前，请给维护者合理的协调披露时间。

## 明确的安全边界

本项目不能替代操作系统级隔离。Codex 的 `workspace-write` 主要限制写入位置，不代表只能读取选定仓库。请阅读 README 的“重要隔离边界”，并优先使用独立 Windows 用户、WSL 或容器身份隔离飞书凭据与 Codex worker。
